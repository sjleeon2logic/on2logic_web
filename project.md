# On2Logic 웹사이트 UI 개편 프로젝트 정리 (Project Summary)

이 문서는 On2Logic 웹사이트(`c:\data\10.src\antigravity_test\on2logic-web`)에 대해 지금까지 진행된 UI 개선 및 기능 수정 작업 내역을 정리한 내용입니다.

## 1. 헤더 (Header) 및 내비게이션 바
- **로고 교체 및 크기 조정**: 기존의 텍스트 기반 로고를 모두 제거하고, 회사의 공식 로고 이미지(`온투로직-로고원본화일.avif`)를 삽입했습니다. 시인성을 높이기 위해 로고 이미지의 높이를 적절히(80px) 키웠습니다.
- **최상단 스크롤 링크**: 좌측 상단 로고 이미지 자체에 클릭 이벤트를 주어, 클릭 시 언제든 페이지 가장 위(최상단)로 부드럽게 돌아갈 수 있도록 설정했습니다.
- **메뉴바 디자인 통일**: 'Company'와 'Business' 글씨의 폰트를 옆의 'Contact Us' 버튼 글씨 모양과 동일하게 맞춘 후 (두께 `600`, 폰트 `var(--font-heading)`), 추가로 1.2배(1.2rem) 키워 가독성을 개선했습니다.
- **버튼 스타일 제거**: 메뉴에 있던 'Contact Us' 항목의 버튼 스타일(외곽선 및 박스)을 직관적인 텍스트 링크 형태로 변경하여, 다른 메뉴 항목들과 통일감을 주었습니다.
- **불필요한 'Strengths' 메뉴 제거**: 동작하지 않던 해당 버튼을 데스크톱/모바일 내비게이션에서 삭제했습니다.

## 2. 메인 히어로 구역 (Hero Section)
- **버튼 순서 및 스타일 변경**: 페이지 아래의 변경된 섹션 순서에 맞도록 'Explore Solutions'와 'Our Story' 두 버튼의 순서를 `[ Our Story ] [ Explore Solutions ]`로 서로 맞바꾸었습니다.
- 'Our Story' 버튼에도 강조 효과(청록색 그라데이션, `.btn-primary` 클래스)를 넣어 가시성을 높였습니다.

## 3. About On2Logic 섹션 (재배치 및 추가)
- **위치 이동**: 페이지 하단 부근에 있던 'About On2Logic' 덩어리 전체를 위로 끌어올려 메인 화면 바로 아래 순서로 배치했습니다.
- **섹션 내부 구조 변경**: 기존 페이지의 'ABOUT US', 'Expert', 'DIPS 1000+ 2024' 관련 카드 3개를 한 묶음으로 나란히 배치했습니다.
- **이미지 자동 크기 최적화**: 'ABOUT US' 글자 아래의 빈 공간을 채우기 위해 자체 제작한 _프리미엄 SoC 설계 컨셉의 3D 추상화 이미지_(`about_us_soc.png`)를 삽입했습니다. 이 카드의 속성을 Flex 박스로 변경 및 `flex-grow: 1` 처리하여, 카드의 세로 크기에 맞춰 빈 여백 없이 자연스럽게 남는 공간을(일부 찌그러짐 없이 자르면서) 꽉 채우도록 수정했습니다.
- **모달(창) 기능 도입**: 'DIPS 1000+ 2024' 카드를 누르면 나오는 모달에서 상단의 텍스트('2024 DIPS 1000+ Designation')를 삭제하고, Assets 폴더에 있는 현판 이미지 2개(`현판1_edited.avif`, `현판2_edited.avif`)가 나타나도록 수정했습니다.

## 4. Why On2Logic 섹션 (레이아웃 개선)
- **정렬 및 폭 최적화**: 해당 영역의 코드가 전체 레이아웃을 잡아주는 `<div class="container">` 바깥으로 밀려나 있어 폭이 무한정 넓어지는 문제를 수정했습니다. 코드를 컨테이너 안으로 묶어 넣어서 위에 배치된 'Core Business Areas' 카드들과 양측 너비가 딱 맞도록 정렬했습니다.
- **여백 축소**: 바로 윗 섹션(Core Business Areas)과 과도하게 떨어져 보이지 않도록 상단 여백(`padding-top`) 값을 6rem에서 1rem으로 큰 폭으로 줄여, 부드러운 화면 흐름을 유도했습니다.

## 5. 푸터 (Footer) 및 하단 연락망
- **로고 제거**: 맨 아래 공간을 차지하던 로고 이미지를 깔끔하게 삭제하고 회사 이름을 온전히 텍스트로 표기(`㈜온투로직 ( On2Logic Co., Ltd. )`)하도록 구조를 조정했습니다.
- **지도 연동**: 푸터에 있는 회사 주소를 누르면 새 창을 통해 직접 네이버 지도의 위치 페이지로 연결되도록 하이퍼링크를 삽입했습니다.
- **이메일 변경 및 효과**: 기존 `info@on2-logic.com` 주소를 `info@on2logic.com`으로 수정했습니다. 또한 마우스를 올렸을 때 클릭 가능하다는 것을 알려주는 청록색 반전(Hover) 효과를 삽입했습니다.
- **전화 연락처 링크**: 모바일 화면에서 전화번호(`+82-31-8039-5800`)를 누르면 바로 전화 통화 화면으로 전환되도록 링크(`tel:`)를 추가했습니다. 이 또한 메일링크와 동일하게 Hover 시 청록색으로 변하는 효과를 주었습니다.

## 6. 스크롤 동작 최적화 (UX 개선)
- 상단 내비게이션 바가 화면 맨 위에 고정되는 형태(Fixed Navbar)인 점을 고려하여, CSS 파일에서 `scroll-padding-top: 100px;` 설정을 추가했습니다. 이로써 메인 메뉴에서 링크를 클릭해 아래 섹션으로 이동할 때, 상단 메뉴바가 섹션의 제목을 가리지 않고 자연스럽게 밑으로 내려오도록 교정했습니다.
- **스크롤 최상단 이동 버튼 (Scroll to Top)**: 사용자가 아래로 스크롤 시 화면 좌측 우하단(초기 좌측하단 요청에서 최종 **우측하단**으로 변경)에 나타나, 클릭하면 즉시 페이지 맨 위로 이동시켜 주는 플로팅 버튼(화살표 모양, 65px 원형 사이즈)을 생성하고 애니메이션 효과를 부여했습니다.
